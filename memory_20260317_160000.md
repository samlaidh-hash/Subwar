# Session 2026-03-17 — controls + seabed note

## Controls change
- **A/D:** submarine yaw (double-tap drag turn still uses `dragTurnLeft`/`dragTurnRight` + multiplier).
- **Mouse (follow + pointer lock):** `handleCameraMouseMove` only — chase cam orbits; maneuver icon forced center each frame.
- **Free cam (K / RMB):** submarine ignores mouse for steering.

## Seabed / mission area size
- `SimpleTerrain.terrainSize = 70000` → **70 km × 70 km** seabed footprint in **X/Z** (centered on scene origin in `simple_terrain_fix.js`).
- Visible “patch” under the sub depends on **camera distance, 75° FOV, fog/clipping** — not a fixed km value in code.

## Files
- `js/submarine.js` — `handleMouseMove`, `updateManeuverControl`
- `index.html` — help panel text
